---
title: "Environments"
description: "Separate test and live modes so staging never emails real users."
icon: 'flask'
---

This page documents SenderKit's test and live environments — how the `sk_test_` and
`sk_live_` key prefixes select a mode, how data and templates are isolated between
them, and how published versions differ across modes. Content to be expanded.
