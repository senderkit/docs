---
title: "Sending"
description: "How SenderKit dispatches messages asynchronously."
icon: 'paper-plane'
---

This page documents the SenderKit send model — submitting a template-based or raw
send, the asynchronous `202 queued` response, and how delivery happens out of band
with retries. It will also contrast template sends with inline raw content. Details
to be expanded from the API and dispatch source.
