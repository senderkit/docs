---
title: "Variables"
description: "Typed variables interpolated into templates at send time."
icon: 'brackets-curly'
---

This page documents typed template variables — defining the shape of values like
`{{name}}` or `{{dashboardUrl}}`, including nested objects and arrays, and how they
are validated and interpolated before a send goes out. Specifics to be filled in
from the variable definitions in the product source.
