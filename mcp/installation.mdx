---
title: "Installation"
description: "Configure the SenderKit MCP server in your AI client."
icon: 'plug'
---

This page documents configuring the SenderKit MCP server in popular clients. The
tabs below are placeholders; exact configuration will be filled in from the CLI's
MCP install flow.

<Tabs>
  <Tab title="Claude Desktop">
    {/* TODO: config from senderkit-sdk/packages/cli/src/mcp/install.ts */}
    Configuration steps for Claude Desktop will go here.
  </Tab>
  <Tab title="Cursor">
    {/* TODO: config from senderkit-sdk/packages/cli/src/mcp/install.ts */}
    Configuration steps for Cursor will go here.
  </Tab>
  <Tab title="Claude Code">
    {/* TODO: config from senderkit-sdk/packages/cli/src/mcp/install.ts */}
    Configuration steps for Claude Code will go here.
  </Tab>
</Tabs>
