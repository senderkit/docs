---
title: "templates"
description: "List and inspect templates from the terminal."
icon: 'file-lines'
---

This page documents the CLI `templates` command group — listing templates and
fetching a single template's detail. Subcommands and flags will be documented from
the source; nothing is guessed here.

{/* TODO: read flags from senderkit-sdk/packages/cli/src/core/commands/templates-list.ts and templates-get.ts */}
