---
title: "send"
description: "Dispatch a message from the terminal."
icon: 'paper-plane'
---

This page documents the CLI `send` command group — dispatching both template-based
and raw sends from the terminal. Subcommands, arguments, and flags will be
documented from the source; nothing is guessed here.

{/* TODO: read flags from senderkit-sdk/packages/cli/src/core/commands/send.ts and send-raw.ts */}
